---
phase: initial-development
task: testing-and-fixes
total_tasks: ~10
status: in_progress
last_updated: 2026-03-09T23:57:25.281Z
---

<current_state>
All fixes and tests are synced to the repo and committed. lib/hook.js is the source of truth,
deployed to ~/.claude/hooks/claude-notify.js. 80-test TDD suite in test/ with npm test scripts.
62 unit tests pass reliably; integration/E2E tests are flaky when run back-to-back due to
ntfy.sh rate limiting (pass individually). Notifications show "rookslog [SSH: dionysus]"
matching the VS Code window title. Ready to discuss design improvements.
</current_state>

<completed_work>

- 80-test TDD suite across 3 tiers (all passing):
  - Unit tests (62): summarize, getProjectName, buildNotification, buildClickUrl, shouldDebounce, loadConfig, edge cases, security
  - Integration tests (9): ntfy.sh round-trip with title/body/priority/tags/click/unicode/disabled/empty-topic/timeout
  - E2E tests (9): full stdin pipeline, multi-window CWD, debounce, error resilience
  - Tests currently at ~/.claude/hooks/test/ — need to move to repo test/

- Bug fixes applied to deployed hook (NOT yet in lib/hook.js):
  1. summarize(): separators → em dash (" — "), newlines → pipe (" | "), list markers → bullet ("•"), maxLen 200→300
  2. getProjectName(): use cwd directly, don't skip HOME, ignore config.workspace_root
  3. buildNotification(): title format changed from "Claude Code · {project}" to "{folder} [SSH: {hostname}]"
  4. buildClickUrl(): prefer cwd over config.workspace_root for multi-window correctness
  5. shouldDebounce(): sanitize session_id to prevent path traversal (replace non-alnum with _)
  6. sendNtfy(): switched from HTTP headers to JSON body mode — fixes unicode corruption in titles

- require.main === module guard + module.exports added for test importability
- GSD model_profile set to quality (opus for debugger agents)
- Signal logged: MODEL_PROFILES prefix mismatch (gsd- vs gsdr- keys in gsd-tools.js)
</completed_work>

<remaining_work>

## Design discussions needed (user wants to discuss before implementing)
- npm package structure finalization and CLI design
- Version control practices and CLAUDE.md updates
- Different notification types for different events (permission vs idle vs elicitation vs stop)
- Quick-response from notifications (actionable notifications for AskUserQuestion tool use)
- Notification importance levels / categories
- Whether to make the repo public
- How to handle multiple concurrent sessions from different VS Code windows

## Cleanup
- Remove workspace_root from config schema (no longer used by getProjectName or buildClickUrl)
- Delete or repurpose vestigial ~/scripts/claude-notify.sh (sources JSON as shell vars — broken)
- Update existing docs/DESIGN.md and docs/TODO.md with new decisions
- Update CLAUDE.md with VC practices (conventional commits, test requirements)

## Pre-publish tasks (from prior session, still valid)
- README.md with install instructions, config reference
- Installer: detect if Claude Code is installed
- Installer: validate ntfy topic is reachable
- Test install flow on clean machine
- GitHub repo creation + first push
- Test runner script (~/scripts/test-claude-notify.sh or npm test)
</remaining_work>

<decisions_made>

- Switched from HTTP-header ntfy mode to JSON body mode — fixes unicode in titles
- Title format is now "{folder} [SSH: {hostname}]" matching VS Code window title
- Per-session cwd is the source of truth for project name (not config.workspace_root)
- List markers translate to bullet character (•) instead of being stripped
- Decorative separators translate to em dash (—) instead of being stripped
- Newlines translate to pipe (|) instead of being collapsed to spaces
- Session IDs sanitized for path traversal safety in debounce file paths
- Test framework: node:test + node:assert (built-in, zero deps, matches project philosophy)
- ntfy.sh polling API used as test oracle for integration/E2E tests (no manual verification needed)
- model_profile set to quality (opus for debug/research agents)
</decisions_made>

<blockers>

- Integration/E2E tests flaky when run together (ntfy.sh rate limits) — need retry logic or self-hosted ntfy for CI
- Prior session noted "cwd drifts because GSD framework changes it" — we solved this differently (use cwd directly, don't rely on workspace_root), but should verify GSD cwd drift is no longer an issue with the new approach
</blockers>

<context>
This project grew from a quick-task notification hook into a proper npm package. Two sessions
have worked on it:

Session 1 (earlier today): Created project structure, CLI, installer, initial hook, CLAUDE.md,
docs/DESIGN.md, deployed to dionysus. Discovered bugs with title/window focus. Paused with
detailed handoff.

Session 2 (this one, from ~/): Didn't know about the repo — worked directly on the deployed
hook. Built comprehensive test suite (80 tests), fixed 6 bugs, confirmed live notifications
work with correct title. The test suite is the major deliverable — it catches real bugs
(unicode in HTTP headers) that unit tests alone wouldn't find.

The owner wants to discuss notification design before continuing implementation: different
types for different events, actionable notifications (respond from notification), importance
levels. This should happen in the project directory context.
</context>

<next_action>
Resume from: the project directory (~/workspace/projects/claude-notify/).

1. First: sync deployed hook back to lib/hook.js and tests to test/
2. Then: discuss notification design (types, actionable responses, importance levels)
3. Then: VC setup (conventional commits, CLAUDE.md practices, GitHub repo)
4. Then: implement discussed features with TDD
5. Then: npm publish prep
</next_action>
