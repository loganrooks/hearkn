---
phase: initial-release
task: design-discussion
total_tasks: ~5
status: paused
last_updated: 2026-03-10T02:12:33.725Z
---

<current_state>
v0.1.0 published to npm as `hearkn`. GitHub repo is `loganrooks/hearkn`.
All cleanup, CI, README, installer improvements, and rebrand are done.
62/62 unit tests pass across Node 18/20/22 on Ubuntu + macOS.
Ready for design discussion about notification types and feature expansion.
</current_state>

<completed_work>

This session (from resume through publish):

- Synced deployed hook with repo (was already in sync)
- Removed workspace_root from config schema, hook, and installer
- Switched all ntfy calls to JSON body mode (cli.js, install.js — hook already done)
- Updated docs/DESIGN.md with current rationale (cwd, JSON body, truncation length)
- Updated docs/TODO.md with current state
- Added README.md with install instructions, config reference, security notes
- Added GitHub Actions CI (Node 18/20/22, Ubuntu + macOS, all green)
- Added VC conventions to CLAUDE.md (conventional commits, trunk-based, semver)
- Installer: added Claude Code detection (checkClaudeCode)
- Installer: added ntfy topic reachability validation (validateTopic)
- Created GitHub repo (public)
- Rebranded: claude-notify → harkn → hearkn (npm name conflicts with yarn, marked)
- Published hearkn@0.1.0 to npm
- Deleted vestigial ~/scripts/claude-notify.sh
- Removed workspace_root from deployed ~/.claude-notify.conf
</completed_work>

<remaining_work>

## Design discussions (user wants to discuss before implementing)
- Different notification types for different events (permission vs idle vs elicitation vs stop)
- Actionable notifications (respond from phone for AskUserQuestion tool use)
- Importance levels / categories
- Multi-session handling from different VS Code windows
- Multi-agent support beyond Claude Code (Codex, Gemini CLI, OpenCode hook protocols)

## Post-discussion implementation
- Implement discussed notification features with TDD
- Test install flow end-to-end on a clean machine (apollo)
- Uninstaller verification

## Nice-to-have (future)
- Self-hosted ntfy support (auth tokens)
- Other notification backends (Pushover, Slack, desktop-notify)
- Automatic editor detection from env vars
- `npx hearkn update` command
</remaining_work>

<decisions_made>

- Package name: hearkn (after claude-notify → harkn → hearkn due to npm similarity filters)
- GitHub repo: loganrooks/hearkn (public)
- npm account: glutenfreebread (has WebAuthn 2FA — need automation token for CI publishing)
- Trunk-based development on main, conventional commits, semver tags
- CI: GitHub Actions, 6-matrix (Node 18/20/22 × Ubuntu + macOS)
- Installer checks: Claude Code detection + ntfy topic validation (both allow override)
- workspace_root fully removed — cwd is sole source of truth
- All ntfy calls use JSON body mode (not HTTP headers) to avoid unicode corruption
- Config path stays ~/.claude-notify.conf and hook stays claude-notify.js for backward compat
- Future rename of config/hook filenames needs migration logic
</decisions_made>

<blockers>

- npm CI publishing not set up yet — need automation token (WebAuthn doesn't work with CLI OTP)
- Integration/E2E tests flaky when run together (ntfy.sh rate limits)
- No test of install flow on a clean machine yet
</blockers>

<context>
This project grew from a quick-task notification hook (in the Dionysus platform project)
into a standalone npm package. Three sessions have worked on it:

Session 1: Created project structure, CLI, installer, initial hook, docs. Discovered bugs.
Session 2: Built 80-test TDD suite, fixed 6 bugs, confirmed live notifications work.
Session 3 (this one): Cleanup, README, CI, installer improvements, rebrand to hearkn,
npm publish v0.1.0. The owner wants to discuss notification design next — types,
actionable responses, importance levels, multi-agent support.

The project directory is still named `claude-notify` on disk — only the npm package,
GitHub repo, and all internal references are `hearkn`. Could rename the directory too
but it's cosmetic.
</context>

<next_action>
Resume from: design discussion about notification architecture.

1. Discuss: notification types per event (what info to show, how to differentiate)
2. Discuss: actionable notifications (can you respond from phone?)
3. Discuss: importance levels and when to use high-priority
4. Discuss: multi-agent support (what hook protocols do Codex/Gemini/OpenCode use?)
5. Then: implement with TDD based on discussion outcomes
</next_action>
